apiVersion: v1  
kind: Service  
metadata:  
  name: langchain-service  
spec:
  type: NodePort  
  selector:  
    app: langchain-app  
  ports:  
    - protocol: TCP  
      port: 80  
      targetPort: 8000